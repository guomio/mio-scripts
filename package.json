{
  "scripts": {
    "version": "node ./version.js",
    "publish:latest": "pnpm publish --filter @guomio/* --no-git-checks",
    "publish:beta": "pnpm publish --tag beta --filter @guomio/* --no-git-checks"
  },
  "private": true,
  "dependencies": {
    "cross-spawn": "^7.0.3",
    "globby": "^11.0.2",
    "inquirer": "^7.3.3",
    "semver": "^7.3.4"
  }
}
